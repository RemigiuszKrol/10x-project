---
import Layout from "@/layouts/Layout.astro";

// Strona wymaga zalogowania - middleware sprawdzi sesję
const user = Astro.locals.user;

if (!user) {
  return Astro.redirect("/auth/login");
}
---

<Layout title="Moje Plany - PlantsPlaner" showNavbar={true}>
  <main class="min-h-screen bg-gradient-to-br from-green-50 via-emerald-50 to-lime-50">
    <div class="container mx-auto px-4 py-8">
      <div class="max-w-4xl mx-auto">
        <div class="bg-white shadow-xl rounded-2xl border border-green-100 p-8">
          <h1 class="text-3xl font-bold text-gray-900 mb-4">Witaj w PlantsPlaner!</h1>

          <div class="bg-green-50 border border-green-200 rounded-lg p-4 mb-6">
            <p class="text-green-800">
              ✓ Zalogowano pomyślnie jako: <strong>{user.email}</strong>
            </p>
          </div>

          <div class="space-y-4">
            <p class="text-gray-600">To jest placeholder dla strony z listą planów działek.</p>

            <p class="text-gray-600">
              User ID: <code class="bg-gray-100 px-2 py-1 rounded">{user.id}</code>
            </p>
          </div>
        </div>
      </div>
    </div>
  </main>
</Layout>
